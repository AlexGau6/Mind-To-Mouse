# Mind-To-Mouse
This code utilizes python and media pipe to track the users head which controls the users mouse. When the user opens their mouth it clicks the mouse. After running the program the user has to click "c" once their head is centered in the camera and looking straight. This will activate the tracking system. To end it the user must press escape.
